#define IHaveSubdirs

#include <Server.tmpl>

       SRCS = shape.c shm.c multibuf.c mitmisc.c xtest1di.c xtest1dd.c
       OBJS = shape.o shm.o multibuf.o mitmisc.o xtest1di.o xtest1dd.o
   INCLUDES = -I../include -I$(INCLUDESRC) -I$(SERVERSRC)/include
   LINTLIBS = ../../server/dix/llib-ldix.ln ../../server/os/4.2bsd/llib-los.ln
        SUBDIRS = xinput

NormalLibraryObjectRule()
NormalLibraryTarget(ext,$(OBJS))
LintLibraryTarget(ext,$(SRCS))
NormalLintTarget($(SRCS))
MakeSubdirs($(SUBDIRS))
DependSubdirs($(SUBDIRS))

#if HasSaberC
saber_src:
	/**/#load $(CFLAGS) $(SRCS)
#endif

DependTarget()
