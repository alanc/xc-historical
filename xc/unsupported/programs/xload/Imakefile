       INCLUDES = -I$(TOP) -I$(TOP)/X11
   INSTALLFLAGS = $(INSTUIDFLAGS)
           OBJS = xload.o
           SRCS = xload.c

SingleProgramTarget(xload,$(OBJS),$(XAWLIB) $(XTOOLLIB) $(XLIB),)
DependTarget()
