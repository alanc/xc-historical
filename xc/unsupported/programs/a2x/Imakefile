XCOMM $XConsortium: Imakefile,v 1.8 92/09/09 20:41:42 rws Exp $

/* define one or more of the following */
#define BuildWithXTest
/* #define BuildWithXTrap */
/* #define BuildWithXTestExt1 */

#ifdef BuildWithXTest
XTESTDEFS = -DXTEST
#ifdef NormalLibXTest /* test if pre-R6 config files */
XTESTGENLIB = $(XTESTLIB)
XTESTGENDEPLIB = $(DEPXTESTLIB)
#else
XTESTGENLIB = -lXtst
#endif
#endif
#ifdef BuildWithXTrap
XTRAPDEFS = -DXTRAP
XTRAPGENLIB = -lXTrap $(XTOOLLIB)
XTRAPGENDEPLIB = $(DEPXTOOLLIB)
INCLUDES = -I$(INCDIR)
#endif
#ifdef BuildWithXTestExt1
XTESTEXT1DEFS = -DXTESTEXT1
#endif

        DEFINES = $(XTESTDEFS) $(XTRAPDEFS) $(XTESTEXT1DEFS)
        DEPLIBS = $(DEPXMULIB) $(XTESTDEPGENLIB) $(XTRAPDEPGENLIB) $(DEPXLIB)
LOCAL_LIBRARIES = $(XMULIB) $(XTESTGENLIB) $(XTRAPGENLIB) $(XLIB)
  SYS_LIBRARIES = -lm

SimpleProgramTarget(a2x)

InstallNamedProg(DragonDictate,DragonDictate,$(BINDIR))

a2x.PS: a2x.ms
	$(TROFF) $(MSMACROS) a2x.ms > $@

clean::
	$(RM) a2x.PS
