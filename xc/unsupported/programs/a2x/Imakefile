#ifndef BuildWithXTrap
GENLIB = $(XTESTLIB)
GENDEPLIB = $(DEPXTESTLIB)
#else
DEFINES = -DXTRAP
GENLIB = -lXTrap $(XTOOLLIB)
GENDEPLIB = $(DEPXTOOLLIB)
#endif

        DEPLIBS = $(DEPXMULIB) $(DEPGENLIB) $(DEPXLIB)
LOCAL_LIBRARIES = $(XMULIB) $(GENLIB) $(XLIB)
  SYS_LIBRARIES = -lm

SimpleProgramTarget(a2x)
