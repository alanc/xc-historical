       INCLUDES = -I$(TOP)
LOCAL_LIBRARIES = $(XAWLIB) $(XMULIB) $(XTOOLLIB) $(EXTENSIONLIB) $(XLIB)
  SYS_LIBRARIES = -lm
           SRCS = xbiff.c
           OBJS = xbiff.o

ComplexProgramTarget(xbiff)
